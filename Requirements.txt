# --- Backend core ---
Flask>=2.3,<3.1
flask-cors>=4.0,<5
requests>=2.32,<3

# If you use OCR, these are needed (but paddlepaddle / paddlepaddle-gpu must be installed manually per OS/CUDA).
# Keep them here only if your local env already has the correct paddlepaddle wheel installed.
paddleocr>=2.7,<3
opencv-python>=4.8,<5
numpy>=1.24,<3
